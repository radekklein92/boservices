// Výchozí stav modal slotu = nic (žádný modal). Modal se vykreslí jen když
// intercepting route (.)prodejny/[locationId] zachytí měkkou navigaci.
export default function Default() {
  return null;
}
